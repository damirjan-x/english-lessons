# Режим разработки: монтирование кода и статики, изменения без пересборки.
# Скопируйте в .env и запускайте: docker compose up
# Для «продакшена» (код только из образа) закомментируйте или удалите строку ниже.
COMPOSE_FILE=docker-compose.yml;docker-compose.dev.yml
